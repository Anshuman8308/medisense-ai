# ─────────────────────────────────────────────────────────────────────────────
# MediSense AI v2 — Python Dependencies
# Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0            # production WSGI server (Render / Railway)

# ── Environment & config ──────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── ML — core ─────────────────────────────────────────────────────────────────
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2

# ── Image processing (CNN inference + PDF) ────────────────────────────────────
Pillow==10.4.0

# ── PDF report generation ─────────────────────────────────────────────────────
reportlab==4.2.2

# ── HTTP client (Google Maps, external APIs) ──────────────────────────────────
requests==2.32.3

# ── ML — CNN  (uncomment ONE block when you're ready to train/infer CNN) ──────
#
# Option A — TensorFlow CPU (lighter, recommended for most servers)
# tensorflow-cpu==2.17.0
#
# Option B — Full TensorFlow (GPU-enabled if CUDA present)
# tensorflow==2.17.0
#
# Option C — PyTorch (alternative; requires matching torchvision)
# torch==2.3.1
# torchvision==0.18.1

# ── Optional: MongoDB persistence ─────────────────────────────────────────────
# pymongo==4.8.0
# flask-pymongo==2.3.0

# ── Optional: rate limiting ───────────────────────────────────────────────────
# flask-limiter==3.7.0
